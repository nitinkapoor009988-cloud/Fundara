[package]
name = "campaign-factory"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
campaign-core = { path = "../campaign-core" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
